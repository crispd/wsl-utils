# To normalize the working tree so that all files adopt a new rule, run:
#   git add --renormalize .
# Then commit the result.

* text=auto

# Force LF for Linux config
wsl-base/etc/wsl.conf text eol=lf
wsl-base/etc/fstab text eol=lf
*.service text eol=lf
*.conf text eol=lf
*.sh text eol=lf
*.bash text eol=lf

# Force CRLF checkout for Windows scripts
*.ps1 text eol=crlf
*.psm1 text eol=crlf